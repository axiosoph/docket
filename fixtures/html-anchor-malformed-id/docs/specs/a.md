# Spec

<a id="Not_Valid"></a>

An html anchor whose id fails the lowercase-kebab grammar.

<a id=""></a>

An html anchor whose id is empty — the pair is still closed and empty,
which is what confirms this was an attempted definition rather than an
ordinary anchor with no `id` at all.

### [registered-claim]

A normal, well-formed, registered definition, present only for
contrast: it stays completely silent.

```claim
kind: requirement
evaluator: test
```
