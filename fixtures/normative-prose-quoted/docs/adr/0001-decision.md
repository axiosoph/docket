# ADR 0001: retry policy

> The rejected proposal said the service MUST retry on failure.

We rejected that: retries mask the underlying fault. Note `MUST` above
appears only as a quoted excerpt of the rejected proposal, not an
assertion this document makes.
