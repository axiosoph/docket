# System overview

### [system-boundary]

The system boundary sits between the atom store and the build graph, per
[lock-groundness](lock-groundness).

```claim
kind: requirement
evaluator: none
cites: [lock-groundness]
```
