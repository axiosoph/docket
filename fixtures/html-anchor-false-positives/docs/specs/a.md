# Spec

See <a href="https://example.com">this external link</a> for background
— an ordinary anchor with no `id` attribute, never a definition
candidate.

<a id="never-closed"> and prose that never provides a matching close
tag right after it.

<a id="has-content">visible text between the tags</a> — real content,
not the invisible-empty shape this form requires.

### [registered-claim]

A normal, well-formed, registered definition, present only for
contrast: it stays completely silent.

```claim
kind: requirement
evaluator: test
```
