# Unregistered definitions (coverage count)

**[unregistered-bold]**: A real bold-form definition with no `claim`
block yet — the normal starting state a registration effort exists to
move.

### [unregistered-heading]

A real heading-form definition with no `claim` block either — the
coverage count is generalized across both forms.

### [registered-heading]

```claim
kind: constraint
evaluator: test
```
